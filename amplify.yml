version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20.16
        - nvm use 20.16
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /www
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
